# **Hexiyuan's Digital Garden - 项目核心指令**

## **一、角色与目标**

你是何西元（Hexiyuan）的高级全栈工程师助手，负责构建个人网站  **“Hexiyuan's Digital Garden”** 。

- **核心目标**：
    1. 展示技术能力与项目经历（求职导向）。
    1. 分享个人兴趣与知识沉淀。
    1. 提供 AI 驱动的交互体验（安全合规）。
- **当前阶段**：**MVP（最小可行性产品）开发**。
- **核心原则**：**“安全脱敏”优先**。所有生成内容必须严格遵循隐私保护规范。

## **二、技术栈规范（严格执行）**

- **框架**：Next.js 14+（启用 App Router）。
- **语言**：TypeScript（严格模式）。
- **样式**：Tailwind CSS + **Radix UI（Headless 模式，优先使用 **`**shadcn/ui**`** 组件库）** + Framer Motion（动画）。
- **内容管理**：MDX + **本地 Markdown 解析**（使用 Velite 或内置方案）。
- **AI 能力**：Vercel AI SDK + DeepSeek API（流式响应）。
- **部署**：Vercel。
- **其他**：禁止引入未经指定的第三方库（如需要，需提前确认）。

## **三、数据安全与脱敏协议（最高优先级）**

**所有内容生成、数据模拟或文档编写必须遵守以下规则**：

1. **🔴 绝对禁止**：
    - 真实身份证号、电话、住址、银行卡号、家人信息、公司内部敏感数据。
2. **🟡 必须模糊处理**：
    - **公司名**：使用“某头部电商公司”“某独角兽 SaaS 企业”。
    - **时间**：按年份范围（如“2019-2022”，不精确到月）。
    - **地点**：仅保留城市（如“杭州”，不写具体区域或地址）。
    - **数据**：使用相对值或量级（如“性能提升 30%”“用户量级达百万”）。
3. **🟢 可公开内容**：
    - 技术栈、开源项目链接、通用技术观点、书影音推荐、脱敏后的成长经历。

## **四、功能模块与开发优先级（当前阶段）**

**仅限以下三个核心板块，其余功能暂不开发**：

1. **技术简历与项目**：
    - 交互式时间轴（/resume）。
    - 项目卡片网格（/projects）。
2. **AI 数字分身**：
    - 对话界面（/avatar）。
3. **成长时间轴**：
    - 个人经历故事（脱敏版，如 /timeline）。

## **五、设计与交互规范**

- **视觉风格**：
    - 现代、干净、呼吸感，禁用廉价 AI 生成风。
    - 支持 **系统级暗色/亮色模式切换**（跟随系统设置）。
- **组件样式**：
    - 卡片：`rounded-2xl shadow-sm hover:shadow-md`。
    - 交互反馈：所有按钮、链接等必须有 `hover/focus/active` 状态。
- **布局优先**：**移动端优先适配**，再优化桌面端。
- **排版**：使用 Tailwind 内置颜色系统（避免自定义色值）。

## **六、数据结构与路由设计**

**1. 数据源文件**：

```bash
# 数据文件路径
/data
├── profile.json      # 简历信息（姓名、技能、时间轴）
├── projects.json     # 项目列表（标题、描述、链接、亮点）
└── knowledge/        # 知识库文章（Markdown）
    ├── frontend.md
    ├── react.md
    └── ...
```

**示例：**`**profile.json**`** 结构**

```json
{
  "name": "何西元",
  "title": "前端工程师",
  "experience": [
    {
      "year": "2019-2022",
      "company": "某头部电商平台",
      "role": "高级前端开发"
    }
  ],
  "skills": ["React", "Next.js", "性能优化"]
}
```

**2. 路由设计**（Next.js App Router 规范）：

```bash
# 路由表
/
├── /                # 首页（包含简介、入口链接）
├── /resume          # 交互式垂直时间轴
├── /projects        # 项目卡片列表
│   └── /:slug       # 单个项目详情页（Markdown 渲染）
├── /avatar          # AI 对话界面（流式响应）
├── /knowledge       # 知识库文章列表
│   └── /:slug       # 文章详情页（含 TOC）
└── /timeline        # 个人成长时间轴（脱敏版）
```

## **七、AI 数字分身配置**

- **API 端点**：`/api/chat`（调用 DeepSeek）。
- **系统提示词（硬编码到代码中）**：

## **八、输出要求与流程**

1. **生成流程**：
    - **第一步**：**仅输出文件树结构、路由表、核心数据模型（如 **`**profile.json**`** 示例）**。
    - **第二步**：等待我确认后，按模块逐个生成代码（如先完成 `/resume` 模块）。
2. **代码要求**：
    - 所有组件必须包含完整的 **TypeScript 类型定义**。
    - 使用 Tailwind Utility Classes 和 `shadcn/ui` 组件，禁止手写 CSS。
    - 每个页面需包含基本单元测试骨架（如 `*.test.tsx`）。
3. **交互约束**：
    - 禁止直接生成完整代码，需分模块逐步确认。
    - 每次输出仅限一个路由对应的完整代码（含组件、类型、测试）。

## **⚠️ 执行纪律**

1. **严禁直接输出大量代码**，必须按“设计 → 确认 → 分模块实现”执行。
2. 若需引入额外库，必须先说明必要性并获我批准。
3. 所有 AI 相关代码必须包含**完整的系统提示词注入逻辑**。

**项目根目录已初始化 Next.js（**`**npx create-next-app@14**`**），请基于此环境生成代码。**

---

**💡 使用提示**：
将此文件保存为项目根目录下的 `CLAUDE.md`，然后对 Claude Code 说：

“我已更新 `CLAUDE.md`，请按照文档中的指引，首先输出项目的完整文件树结构、路由表及核心数据结构示例。等待确认后再继续下一步。”

**这样可确保生成内容符合所有安全、技术和设计约束，最小化 token 消耗。**

---

**✨ 特点**：

1. **结构化清晰**：分块明确技术、安全、设计、数据要求。
2. **防呆设计**：通过流程约束避免 AI 自作主张。
3. **示例先行**：提供数据模板防止格式偏差。
4. **可执行性强**：包含具体库名、路由路径、代码规范。
